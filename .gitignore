# Local RHSM credentials (do not commit secrets)
etc/management-client/.credentials

# Optional local env files
.env
.env.*
*image-mode.txt

# Local image build secrets (Telegram); template: exam-build.env.example
exam-build.env
