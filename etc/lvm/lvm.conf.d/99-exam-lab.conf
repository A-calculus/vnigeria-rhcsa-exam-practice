# Exam lab (container): RHEL default use_devicesfile=1 only tracks PVs in
# /etc/lvm/devices/system.devices. Learner PVs on /dev/sd*1 (loop) are not listed,
# so vgs/vgchange may omit or reject new VGs while vgdisplay <name> still works.
devices {
    use_devicesfile = 0
}
