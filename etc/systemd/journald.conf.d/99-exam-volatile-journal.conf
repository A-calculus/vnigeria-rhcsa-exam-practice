# Avoid persistent /var/log/journal when /var/log is a bind mount (e.g. host setup.sh).
# Journal then lives under /run/log/journal only, so poweroff does not fail unmounting
# var-log-journal.mount while journald still holds files open.
[Journal]
Storage=volatile
